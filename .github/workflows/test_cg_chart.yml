name: Test CoinGecko Chart

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run single CoinGecko test
        run: |
          python3 -m pip install requests
          python3 tests/test_cg_chart_single.py
